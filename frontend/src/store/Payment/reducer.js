import {
  } from 'store/actions'
import {  } from './actions';
  
  const initialState = {
    paymentlist : null,
  }
  
  const paymentReducer = (state = initialState, action) => {
    switch(action.type) {
      default:
        return state
    }
  }
  
  export default paymentReducer
  